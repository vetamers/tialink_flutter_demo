/// Mockito cannot generate a valid mock class which implements 'BluetoothConnection' for the following reasons:
///     The property accessor 'BluetoothConnection.output' features a private return type, and cannot be stubbed.
///     The property accessor 'BluetoothConnection.output=' features a private parameter type, '_BluetoothStreamSink', and cannot be stubbed.
void main() {

}